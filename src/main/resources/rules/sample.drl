package rules;

import com.example.demo.model.drool.Person;

rule "Adult Rule"
    when
        $p : Person(age >= 20)
    then
        System.out.println($p.getName() + " is an adult.");
        $p.setResult(true);
end